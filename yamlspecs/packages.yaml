---
site: !include site.yaml
system:
  - openmpi
  - libX11-devel
  - autoconf
  - lua-devel
  - !ifeq "{{site.os_release}},8,perl-Text-Balanced,}}"
bootstrap:
  - gcc-module
  - tcl
  - tcl-module
build:
  - java8-module
  - java11-module
  - intel-compiler-module
  - intel-tbb
  - intel-tbb-module
  - icu
  - icu-module
  - openmpi-module
  - tk
  - tk-module
  - parallel
  - parallel-module
  - likwid
  - likwid-module
  - nodejs
  - !ifeq "{{site.os_release}},8,,protobuf-classic"
  - !ifeq "{{site.os_release}},8,,protobuf-classic-module"
  - !ifeq "{{site.os_release}},8,,setuptools}}"
  - !ifeq "{{site.os_release}},8,,java7-module}}"
manifest:
  - "{{bootstrap}}"
  - "{{build}}"
